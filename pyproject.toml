[project]
name = "ripe-kg"
version = "1.1.0"
description = "RIPE knowledge graph generation and exploration"
requires-python = ">=3.11"
dependencies = [
    "psycopg[binary]>=3.2,<4",
    "rdflib>=7.6.0",
]
